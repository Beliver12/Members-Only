<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/style.css">
  <meta charset="UTF-8">
  <title></title>
</head>
<body>
  <h1>Messages</h1>
  <%- include("errors.ejs") %>
  <nav>
    <ul>
        <% if (locals.messages) {%>
          
        <% for (let i = 0; i < messages.length; i++) { %>
          <li>
          <% if (user.rolestatus  === null || user.rolestatus === 0) {%>
            <p> <strong>User<%= messages[i].userid %></strong></p>
           <p><%= messages[i].message %></p>
           
           <% } else if (locals.user.rolestatus  === 1) { %>
           
            <p><strong><%= messages[i].username %></strong> &nbsp;&nbsp;<span id="date"><%= messages[i].date.toDateString() %></span></p>
            
           <p><%= messages[i].message %></p>
           
            <%} else { %>
              <p><strong><%= messages[i].username %></strong> &nbsp;&nbsp;<span id="date"><%= messages[i].date.toDateString() %></span></p>
            
           <p><%= messages[i].message %></p>
           <form id="delete" action="/delete/<%= messages[i].id %> " method="POST" style="display:inline;">
            <button type="submit" onclick="return confirm('Are you sure you want to delete this message?' )">Delete</button>
            </form>
            <%}%>
          </li>
            <% } %>
       <% } %>
    </ul>
  </nav>

  <div id="links">
    <a href="/log-out">LOG OUT</a>
    <a href="/create-message">Create Message</a>
    <a href="/">Home</a>
  </div>
</body>
</html>
